/**
 * {{MODULE}} Schemas
 *
 * Define request and response schemas using Zod.
 */

import { z } from "zod/v4";

export const {{MODULE}}ResponseSchema = z.object({
  success: z.literal(true),
  message: z.string(),
  data: z.unknown().nullable(),
});

export type {{CLASS}}Response = z.infer<typeof {{MODULE}}ResponseSchema>;
