import type { PropsWithChildren } from 'react';
import { Pressable, Text, TextInput, View } from 'react-native';
export function Button({ children, onPress, variant = 'primary', disabled }: PropsWithChildren<{onPress?:()=>void;variant?:'primary'|'secondary'|'ghost';disabled?:boolean}>) { return <Pressable disabled={disabled} onPress={onPress} className={`items-center rounded-lg px-4 py-3 ${variant === 'primary' ? 'bg-maroon' : variant === 'secondary' ? 'border border-border bg-card' : ''} ${disabled ? 'opacity-50' : ''}`}><Text className={`font-semibold ${variant === 'primary' ? 'text-white' : 'text-foreground'}`}>{children}</Text></Pressable>; }
export function Field({ value, onChangeText, placeholder, secureTextEntry, keyboardType }: {value:string;onChangeText:(v:string)=>void;placeholder:string;secureTextEntry?:boolean;keyboardType?:'default'|'email-address'|'numeric'}) { return <TextInput className="rounded-lg border border-border bg-card px-4 py-3 text-base text-foreground" placeholderTextColor="#79747e" value={value} onChangeText={onChangeText} placeholder={placeholder} secureTextEntry={secureTextEntry} keyboardType={keyboardType}/>; }
export function Card({children}:PropsWithChildren) { return <View className="rounded-xl border border-border bg-card p-5">{children}</View>; }
export function Screen({children}:PropsWithChildren) { return <View className="flex-1 bg-background px-5 pt-14">{children}</View>; }
