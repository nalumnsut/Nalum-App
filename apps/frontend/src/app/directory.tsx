import { useState } from 'react'; import { ActivityIndicator, Pressable, Text, TextInput, View } from 'react-native'; import { FlashList } from '@shopify/flash-list'; import { Link } from 'expo-router'; import { useQuery } from '@tanstack/react-query'; import { usersApi, type User } from '@/lib/api'; import { Card, Screen } from '@/components/ui/nalum';
function Person({user}:{user:User}){const p=user.profile;return <Card><Text className="text-lg font-semibold text-foreground">{user.firstName} {user.lastName}</Text><Text className="mt-1 text-muted">{p?.currentRole ?? 'NSUT community member'}{p?.currentCompany ? ` · ${p.currentCompany}`:''}</Text><Text className="mt-2 font-mono text-xs text-maroon">{p ? `${p.batch} · ${p.branch} · ${p.campus}` : user.role}</Text></Card>}
export default function Directory(){const[q,setQ]=useState('');const{data,isLoading,error,refetch}=useQuery({queryKey:['directory',q],queryFn:()=>usersApi({q:q||undefined,limit:20,offset:0})});return <Screen><View className="mb-5 flex-row items-center justify-between"><View><Text className="text-3xl font-bold text-foreground">Directory</Text><Text className="text-muted">Find the Nalum community</Text></View><Link href="/profile" className="font-semibold text-maroon">My profile</Link></View><TextInput className="mb-4 rounded-lg border border-border bg-card px-4 py-3 text-foreground" placeholder="Search people, companies, or cities" placeholderTextColor="#79747e" value={q} onChangeText={setQ}/>{isLoading?<ActivityIndicator color="#7a1f35"/>:error?<Pressable onPress={()=>refetch()}><Text className="text-maroon">Could not load directory. Tap to retry.</Text></Pressable>:<FlashList data={data?.users ?? []} renderItem={({item})=><View className="mb-3"><Person user={item}/></View>} ListEmptyComponent={<Text className="mt-8 text-center text-muted">No members match your search.</Text>}/>}</Screen>}
